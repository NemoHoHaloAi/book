# PRML学习笔记

## 绪论
模式识别：利用计算机算法自动发现**数据**中的**规律**，并基于规律进行**分类回归**任务；与传统的数学/逻辑推理不同，模式识别忽略过程的严谨性，更关注结果，抛弃理想状态，拥抱混沌；

算法类型：
- 有监督学习：输入对应明确的输出标签，例如分类/回归问题；
- 无监督学习：输入不对应输出标签，例如聚类、降维、密度估计等；
- 强化学习：给定环境下，通过执行不同动作，实现最终奖励最大化；

核心工具：概率论、决策论、信息论；

#### 多项式拟合案例
1. 使用 $$sin(2πx)+高斯随机噪声$$ 构建训练数据集，输入为**x**，标签为**t**；
2. 确定多项式形式为 $$y(x,w)=w_0x^0+w_1x^1+....+w_Mx^M=\sum^M_{j=0}{w_jx^j}$$ ，其中M为多项式的阶数，代表模型复杂度或者灵活度；
3. 误差函数选MSE，公式为 $$E(w)=\frac{1}{2}\sum^N_{n=1}(y(x_n,w)-t_n)^2$$，E(w)是w的二次函数，它对w求导后形式为线性函数，因此它具有唯一解并可通过解析法计算得到，记为 $$w^*$$ ；
4. 不同阶数下对应的 $$w^*$$ 函数y与sin(2πx)曲线对比，可以看到随着阶数上升对于训练集拟合的误差趋近于0：<img width="80%" height="40%" alt="image" src="https://github.com/user-attachments/assets/ea681b86-0cca-4458-b107-7446a7c47612" />
5. 理论上来说，阶数更大的多项式至少能够得到与阶数更小的多项式一样好的结果，同时考虑到 $$sin(2πx)$$的幂级数展开包含所有阶数的项，理论上M越大应该越接近原函数，效果更好，从表格可以看到，随着M增加，系数的绝对值也明显变大了：<img width="518" height="358" alt="image" src="https://github.com/user-attachments/assets/517c0647-c72d-4804-aaac-e22d2d3a20b8" />
6. 但是考虑到训练集本身数据量有限，而基于最小化误差的目的去拟合参数，直觉上使得随机噪声也被错误的识别为模式并进行表示并限制了模型的泛华能力；
7. 解决过拟合问题方法：
   1. 减少模型参数量，例如降低M；
   2. 增加训练集数据量，更多的数据驱使更多的模型参数拟合真正的模式而不会有闲置的参数拟合噪声；
   3. 正则化：通过在损失函数中引入惩罚项来限制参数空间范围，例如在MSE中加入惩罚项后形式为 $$E(w)=\frac{1}{2}\sum^N_{n=1}(y(x_n,w)-t_n)^2+\frac{\lambda}{2}(w^Tw)$$；
8. M=3与M=9但是增加正则化，从效果上可能是接近的，但是系数差异较大，因此二者并不是等价的：<img width="474" height="354" alt="image" src="https://github.com/user-attachments/assets/3b313643-b8fe-4629-8c5c-605cfec8611e" />

